/* function fullName() {
  console.log("Susana Horia");
}
fullName(); */

/* function fullName(firstName, lastName) {
  return firstName + " " + lastName;
}
console.log("Susana", "Horia"); */

/* function addNumbers(num1, num2) {
  let result = num1 + num2;
  console.log(result);
}
addNumbers(10, 5); */

/* function areaOfRectangle(largo, ancho) {
  let area = largo * ancho;
  console.log(area);
}
areaOfRectangle(8, 3); */

/* function perimeterOfRectangle(largo, ancho) {
  let perimetro = 2 * (largo + ancho);
  console.log(perimetro);
}
perimeterOfRectangle(7, 3); */

/* function volumeOfRectPrism(largo, ancho, alto) {
  let volumen = largo * ancho * alto;
  console.log(volumen);
}
volumeOfRectPrism(3, 7, 5); */

/* function areaOfCircle(radio) {
  const pi = Math.PI;
  let area = pi * radio * radio;
  console.log(area);
}
areaOfCircle(5); */

/* function circumOfCircle(radio) {
  const pi = Math.PI;
  let area = 2 * pi * radio;
  console.log(area);
}
circumOfCircle(3); */

/* function density(masa, volumen) {
  let densidad = masa / volumen;
  console.log(densidad);
}
density(20, 4); */
